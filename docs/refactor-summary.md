# Refactoring summary
The AI basically went back to the previous version, before the intentional breakages.
So the Locators were fixed to getByRole instead of css based ones.
And the explicit wait and goto by link was changed to the existing method navigate, that handles the navigation well.

I did not have to do anything myself, the version was good :)